pull_request_rules:
  - name: Otomatik Merge Testi
    conditions:
      - base=main
    actions:
      merge:
        method: merge
pull_request_rules:
  - name: Otomatik Merge Testi
    conditions:
      - base=main
    actions:
      merge:
        method: merge
      label:
        add:
          - "mergify-test"
