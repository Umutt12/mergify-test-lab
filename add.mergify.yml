queue_rules:
  - name: default
    conditions:
      - base=main

pull_request_rules:
  - name: PR'ı sıraya al ve merge et
    conditions:
      - base=main
    actions:
      queue:
        name: default
